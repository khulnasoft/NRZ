{
  "name": "cli",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "cargo build --package nrz",
    "build:release": "cargo build --package nrz --profile release-nrz",
    "clean": "cargo clean --package nrz"
  }
}
