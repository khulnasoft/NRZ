{
  "$schema": "https://nrz.build/schema.json",
  "globalDependencies": ["somefile.txt"],
  "tasks": {
    "build": {
      "outputs": ["foo.txt"]
    },
    "test": {
      "outputs": [],
      "dependsOn": ["build"]
    }
  }
}
